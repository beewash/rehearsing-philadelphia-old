import React from 'react';
import { useQuery } from 'react-query';
import { Link } from 'react-router-dom';
import { sanity, imageUrlBuilder } from '../sanity';

const query = `
    
`;

function schedule() {
    return (
        <>
            <h1 className="text-big">Schedule</h1>
        </>
    )
}

export default schedule;